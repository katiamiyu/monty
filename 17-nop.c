#include "monty.h"

/**
 * _nop - Doesn't do anything
 * @stack: Double pointer to the top of the stack
 * @lin_number: Line number in the file
 */

void _nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
